# Optional development tools (0 or 1)
XDEBUG=0
COMPOSER=0

TIME_ZONE=Europe/Rome
WEBAPP_URL=http://localhost:8080/

HTTP_PORT_EXPOSED=8080
DB_PORT_EXPOSED=3306

# Development-only credentials: change them before starting the stack.
DB_ROOTPASS=change-me-root
DB_USER=dbuser
DB_PASS=change-me
DB_NAME=dbapp
